#database properties

spring.datasource.url=jdbc:mysql://localhost:3306/authentication
spring.datasource.username=root
spring.datasource.password=Xworkzodc@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver




#Email properties
# SMTP host (e.g., smtp.gmail.com, smtp.mailtrap.io)
spring.mail.host=smtp.gmail.com

# SMTP port (e.g., 587 for TLS, 465 for SSL)
spring.mail.port=587

# Username for authentication (e.g., your email address)
spring.mail.username=roshanxworkz@gmail.com

# Password for authentication (use an app password for Gmail)
spring.mail.password=bres ckcm ovaf cjjd

# Enable SMTP authentication
spring.mail.properties.mail.smtp.auth=true

# Enable STARTTLS for a secure connection
spring.mail.properties.mail.smtp.starttls.enable=true